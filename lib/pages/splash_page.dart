import 'package:flutter/material.dart';

class ShplashPage extends StatelessWidget {
  const ShplashPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}